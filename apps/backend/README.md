# Adaptly MVP backend

Минимальный API для инвесторского MVP без внешних зависимостей.

## Запуск

```bash
cd apps/backend
python3 server.py
```

По умолчанию API слушает `http://127.0.0.1:8000`.

## Endpoints

- `GET /api/health`
- `GET /api/demo-state`
- `POST /api/progress`

